#include "ReadyQueue.h"

int main()
{
    Engine eng;
    eng.execute();

    // ReadyQueue rq;
    // NodeTask node1(1, false);
    // NodeTask node2(2, false);
    // NodeTask node3(-1, false);

    // rq.push(node1);
    // rq.push(node2);
    // rq.push(node3);
    // rq.dump();
    return 0;
}